import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: 30,
    },
    progressBar: {
        width: '100%',
        flexDirection: 'row'
    }
});

export default styles;