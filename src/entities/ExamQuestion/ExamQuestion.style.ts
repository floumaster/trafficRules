import { StyleSheet } from 'react-native';
import { GRAY, LIGHT_GRAY, WHITE } from 'shared/const';

const styles = StyleSheet.create({
    container: {
        width: '100%',
    },
    text: {
        color: LIGHT_GRAY,
        fontSize: 18,
        lineHeight: 28,
        fontWeight: '500'
    }
});

export default styles;