import { StyleSheet } from "react-native";
import { WHITE, WHITE_BLUE } from "shared/const";

const styles = StyleSheet.create({
   container: {
    backgroundColor: WHITE_BLUE,
    borderWidth: 0,
   },
   text: {
      color: WHITE
   }
});

export default styles;